/**
 * CustomCursor — disabled.
 *
 * The site now uses a CSS-only custom cursor defined in global.scss.
 * This component is kept as a no-op so existing imports don't break.
 */
export default function CustomCursor() {
  return null
}
